.venv/
__pycache__/
*.py[cod]
.DS_Store

# Scraped transcripts and summaries are local artifacts, not repo content.
# scrape.py creates output/ on demand, so the folder need not be in the repo.
output/
.claude/
